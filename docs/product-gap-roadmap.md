# 聚映功能缺口与完善路线图

> 目标：从“单一可用来源 + 原生播放器原型”演进为“多源、可分类、可换源、适合手机追番的完整产品”。  
> 原则：先补数据模型和后端能力，再补 UI；所有按钮必须有真实状态和失败反馈。

## 0. 2026-07-23 已完成批次

- 搜索结果已保留规范作品 ID 和完整 `variants[]`，不会因去重丢失来源 ID。
- 新增 `POST /api/media/detail`，并行聚合来源详情、剧集和同集播放线路。
- 新增自定义移动端播放器核心：选集、上下集、换源、倍速、画中画、全屏、分享、收藏和播放进度。
- 弹幕开关/设置面板已接入真实空状态；缓存按钮保持未启用，不伪装下载能力。
- 已读取 `C:\Users\songz\Desktop\public-work\remote_sources\` 的完整来源脚本；AuvFun、Cycapp、Jinpai、Sanqiu 的 Native 默认流程不再因缺少环境变量而被跳过。

## 1. 功能缺口总表

| 领域 | 当前状态 | 目标状态 | 优先级 |
|---|---|---|---|
| 多源资源 | Lanerc、AuvFun、Sanqiu 已能返回数据，Cycapp/Jinpai 待健康验证 | 13 个来源按 Native/JS/Browser 分批适配 | P0 |
| 多源合并 | 请求内保留 `SourceVariant[]`，可打开来源面板 | 落地持久目录、健康排序和跨请求换源 | P0 |
| 分类目录 | 依赖来源首页文案 | 统一分类、标签、年份、季度、状态和评分索引 | P0 |
| 播放器 | 自定义核心已实现 | 横屏手势、锁屏、清晰度重解析和错误切换 | P0 |
| 选集/下一集 | 播放器内可选集/上下集/连播 | 跨来源集号映射和断点续播完善 | P0 |
| 换源 | 详情聚合后可切换已返回线路 | 多来源测速、健康排序和失败切换 | P0 |
| 倍速 | 0.5x–3.0x 菜单并记忆偏好 | 与原生/原生 App 行为一致 | P0 |
| 画中画 | 能力检测和入口已实现 | 更多 Android WebView 兼容性验证 | P0 |
| 收藏/历史 | localStorage + 播放进度 | 集号/时间点统一、可选云同步 | P1 |
| 分享 | Web Share API + 复制链接降级 | 分享卡片和深链接 | P1 |
| 弹幕 | 空状态面板和设置已实现 | 授权弹幕通道、渲染、屏蔽、发送 | P1 |
| 清晰度 | 来源返回才显示 | AUTO + 真实码率/分辨率 + 切换保留进度 | P1 |
| 缓存 | 未启用 | 仅设备端、来源授权允许时缓存；任务管理 | P2 |
| 热度榜 | 无 | 日榜/周榜/总榜，明确统计口径 | P1 |
| 番剧排期 | 无 | 周一至周日 + 更新时间 + 更新提醒 | P1 |
| 高级筛选 | 无 | 类型、地区、年份、季度、状态、评分、排序 | P1 |
| 账号同步 | 无 | 可选登录、收藏/进度同步、匿名模式保留 | P3 |
| 高并发 | 内存缓存 | 分布式缓存、熔断、限流、后台刷新和指标 | P2 |

## 2. 目标信息架构

### 2.1 一级导航

- 首页
- 分类
- 排期
- 榜单
- 片库/搜索
- 我的

移动端底部导航建议保留 4 个：

- 首页
- 分类
- 排期/榜单
- 我的

搜索始终位于首页和分类页顶部。

### 2.2 首页模块

1. 轮播推荐
2. 今日更新
3. 正在热播
4. 日漫 TV
5. 剧场版
6. 国漫
7. 完结高分
8. 最近更新
9. 热度榜前十
10. 本周排期摘要

每个模块必须来自统一目录 API，不能只依靠某一个来源首页的分区名称。

### 2.3 分类体系

统一筛选维度：

- 内容形态：TV、剧场版、OVA/OAD、短篇、真人影视。
- 地区：日本、中国、欧美、韩国、其他。
- 题材：冒险、奇幻、校园、恋爱、喜剧、悬疑、科幻、运动、音乐、治愈等。
- 年份：今年、近三年、年份区间。
- 季度：冬、春、夏、秋。
- 状态：连载、完结、未开播。
- 评分：9+、8+、7+、全部。
- 排序：热度、评分、更新时间、首播时间。

来源方原始分类先映射到聚映规范标签，无法映射的保留 `rawTags`，不能静默丢弃。

## 3. 目标数据模型

### 3.1 规范作品 `Media`

建议字段：

- `id`
- `title`
- `originalTitle`
- `aliases[]`
- `mediaType`
- `region`
- `year`
- `season`
- `genres[]`
- `status`
- `episodeCount`
- `score`
- `heat`
- `cover`
- `banner`
- `description`
- `airSchedule`
- `variants[]`

### 3.2 来源变体 `SourceVariant`

每个来源结果必须保留：

- `sourceKey`
- `sourceMediaId`
- `sourceTitle`
- `sourceCover`
- `sourceScore`
- `lastCheckedAt`
- `health`
- `episodes[]`

搜索去重只负责把多个 `SourceVariant` 挂到同一个 `Media`，不能再覆盖或丢弃来源 ID。

### 3.3 规范剧集 `CanonicalEpisode`

- `episodeNumber`
- `seasonNumber`
- `title`
- `airedAt`
- `duration`
- `variants[]`

每个剧集来源候选包含：

- `sourceKey`
- `sourceEpisodeId`
- `route`
- `flag`
- `qualityHints`
- `lastPlaySuccessAt`
- `failureCount`

## 4. 多源适配计划

### 4.1 Native Adapter

目标来源：

- Lanerc
- AuvFun
- Cycapp
- Jinpai
- Sanqiu

任务：

1. 为每个 Adapter 建立固定响应 Fixture。
2. 完成首页、搜索、详情、播放四项契约测试。
3. 区分“Adapter 已编写”“已配置”“健康”“播放已验证”四种状态。
4. 将来源专用认证全部封装在 Adapter 内。
5. 输出规范化类型、分类标签、剧集号、质量信息和过期时间。

### 4.2 JS Worker

目标来源：

- 瓜子
- 双星
- 云帆
- 咕咕动漫

要求：

- 独立进程/隔离 Worker。
- 固定允许的宿主 API，不提供任意文件系统和进程能力。
- 网络访问按来源 Manifest 控制。
- CPU、内存、总耗时、响应大小均有限制。
- 远程脚本先下载、校验、版本化和人工审核，再进入运行环境。
- 主 API 只接收规范化 JSON，不接收可执行代码。

### 4.3 Browser Worker

目标来源：

- 稀饭动漫
- AkiAnime
- 动漫在线
- 动漫巴士

要求：

- 独立浏览器池，不与用户浏览器会话共享。
- 域名级并发限制。
- 页面加载、脚本执行和选择器全部有超时。
- 禁止持久 Cookie，除非来源明确授权且有专用凭据。
- DOM 解析结果必须进入同一 Adapter 合约。
- CAPTCHA、登录墙和付费内容不自动绕过。

## 5. 播放器完善规格

### 5.1 控制层

竖屏与横屏都需要：

- 播放/暂停
- 当前时间/总时长
- 可拖动进度条
- 上一集/下一集
- 选集
- 换源
- 清晰度
- 倍速
- 弹幕开关
- 弹幕设置
- 画中画
- 全屏
- 锁定控制
- 更多菜单

移动端横屏额外支持：

- 左侧上下滑调亮度
- 右侧上下滑调音量
- 横向滑动调进度
- 双击左/右区域快退/快进
- 控件自动隐藏

### 5.2 播放状态机

建议状态：

```text
idle
  -> resolving
  -> loading
  -> playing / paused
  -> ended
  -> auto_next

resolving / loading / playing
  -> recoverable_error
  -> switching_source
  -> loading

recoverable_error
  -> fatal_error
```

状态必须记录：

- 当前作品、集号和来源。
- 当前播放时间和时长。
- 当前清晰度和倍速。
- 来源候选列表。
- 是否全屏、画中画、锁屏。
- 弹幕开关和设置。
- 自动下一集倒计时。

### 5.3 下一集

下一集不能只依赖按钮顺序，应使用规范集号：

1. 当前集播放结束。
2. 保存完成状态。
3. 查找 `episodeNumber + 1`。
4. 优先使用当前健康来源。
5. 当前来源无下一集时匹配其他来源。
6. 显示 5 秒倒计时，允许取消。

### 5.4 换源

换源面板展示：

- 来源名称
- 路线名称
- 最近成功率
- 最近解析耗时
- 清晰度能力
- 当前选中状态

自动换源只在网络错误、解析失败或媒体不可播放时触发，不能在用户明确选择来源后无提示切换。

### 5.5 倍速

固定选项：

- 0.5x
- 0.75x
- 1.0x
- 1.25x
- 1.5x
- 2.0x
- 3.0x

偏好保存在设备端，切集时保留。

### 5.6 画中画

- 使用 `document.pictureInPictureEnabled` 和媒体元素能力检测。
- 不支持时隐藏或禁用，并显示原因。
- iOS 使用系统允许的 `playsInline`/原生能力，不伪造入口。

### 5.7 弹幕

弹幕必须有明确的数据来源，不能把评论伪装成弹幕。

目标接口：

- `GET /api/danmaku?mediaId=&episode=`
- `POST /api/danmaku`

设置项：

- 开关
- 透明度
- 字号
- 速度
- 显示区域
- 顶部/底部/滚动类型
- 同屏密度
- 关键词屏蔽
- 用户屏蔽
- 彩色弹幕开关

若没有授权弹幕来源，播放器应显示“暂无弹幕通道”，但设置面板和本地测试通道可以独立开发。

### 5.8 缓存

缓存分为两类：

- 元数据缓存：服务端允许，当前已有基础实现。
- 视频离线缓存：只能在用户设备进行，且来源明确允许、CORS/DRM/协议支持时启用。

服务端仍不保存影片。缓存页需要显示任务、大小、进度、清晰度、失败原因、删除和空间占用。

## 6. 收藏、历史、分享和进度

### 6.1 播放进度

保存字段：

- `mediaId`
- `episodeNumber`
- `sourceKey`
- `position`
- `duration`
- `updatedAt`
- `completed`

写入时机：

- 每 10–15 秒节流写入。
- 暂停、切集、退出、页面隐藏时强制写入。
- 播放到 90% 以上标记完成。

### 6.2 收藏

补齐：

- 收藏/取消收藏。
- 自定义收藏夹。
- 按最近更新、收藏时间、评分排序。
- 有新集时显示角标。

### 6.3 分享

优先使用 Web Share API，降级为：

- 复制当前作品链接。
- 复制“作品 + 集数 + 播放进度”深链。
- 生成分享卡片属于后续功能。

## 7. 榜单与排期

### 7.1 热度榜

不能直接混用不同来源的“热度”字段。建议统一分数：

```text
heatScore =
  0.35 * normalizedSourceHeat +
  0.25 * recentDetailViews +
  0.20 * recentPlayStarts +
  0.10 * favorites +
  0.10 * freshness
```

匿名统计必须聚合、限时保存并提供隐私说明。第一阶段也可以只使用来源方公开榜单，并标记来源。

### 7.2 番剧排期

模型：

- 星期
- 预计更新时间
- 时区
- 作品 ID
- 下一集号
- 来源更新时间
- 是否已更新

展示：

- 周一到周日标签。
- “今天”默认选中。
- 已更新、待更新、时间未知三种状态。
- 支持按地区和内容形态筛选。

## 8. 后端 API 演进

建议新增：

| API | 作用 |
|---|---|
| `GET /api/catalog` | 分类、筛选、排序和分页 |
| `GET /api/media/:id` | 规范作品和全部来源变体 |
| `GET /api/media/:id/episodes` | 规范剧集和来源映射 |
| `GET /api/rankings` | 日榜/周榜/来源榜 |
| `GET /api/schedule` | 番剧排期 |
| `GET /api/sources/health` | 来源状态和能力 |
| `POST /api/play/resolve` | 指定作品、集号和来源解析 |
| `POST /api/play/failover` | 播放失败后选择下一候选 |
| `GET/POST /api/progress` | 进度读取和写入 |
| `GET/POST /api/danmaku` | 弹幕读取和发送 |

## 9. 前端拆分计划

将 `app/page.tsx` 拆分为：

```text
app/
  components/
    catalog/
    media/
    player/
    profile/
    schedule/
    ranking/
  hooks/
    useCatalog.ts
    usePlayer.ts
    usePlaybackProgress.ts
    useFavorites.ts
  lib/
    api-client.ts
    player-state.ts
```

播放器至少拆为：

- `VideoSurface`
- `PlayerControls`
- `EpisodeDrawer`
- `SourceDrawer`
- `QualityMenu`
- `SpeedMenu`
- `DanmakuLayer`
- `DanmakuSettings`
- `PlayerGestures`

## 10. 分阶段实施

### Phase 0：架构修正和真实换源（核心已完成）

- [x] 扩展统一数据模型。
- [x] 搜索结果保留 `SourceVariant[]`。
- [x] 建立规范作品 ID 和剧集映射。
- 把 `app/page.tsx` 拆分为领域组件。
- 建立 Adapter Fixture 和契约测试。

验收：

- 同一作品至少两个来源时能在详情页看到并切换。
- 去重后不丢失任何来源 ID。

### Phase 1：完整移动端播放器（核心已完成）

- [x] 自定义控制层。
- [x] 选集、上下集、倍速、换源、画中画、全屏。
- [x] 播放进度和断点续播。
- [x] 分享功能。
- 播放错误自动/手动换源。
- 清晰度真实重解析、横屏手势和锁屏。

验收：

- Android Chrome 和桌面 Chrome 完成一轮端到端播放。
- 切清晰度/换源时保留播放进度。
- 下一集和历史记录使用真实集号和进度。

### Phase 2：目录、分类、榜单和排期

- 建立元数据目录存储。
- 分类规范化。
- `/api/catalog`、`/api/rankings`、`/api/schedule`。
- 首页模块改为统一目录驱动。

验收：

- TV、剧场版、地区、年份、评分和状态筛选可组合使用。
- 周排期有明确数据来源和更新时间。

### Phase 3：扩大来源

- 完成 4 个 JS Worker 来源。
- 完成 4 个 Browser Worker 来源。
- 来源健康、熔断、限流和人工停用。

验收：

- 每个启用来源通过首页/搜索/详情/播放契约测试。
- 某来源失败不会拖垮整体搜索或播放器。

### Phase 4：弹幕和设备端缓存

- 弹幕协议、渲染和设置。
- 来源允许时的设备端离线缓存。
- 缓存任务管理和空间控制。

验收：

- 没有弹幕数据时显示真实空状态。
- 视频不进入聚映服务端存储。

### Phase 5：生产高并发和账号同步

- 分布式缓存。
- 后台增量同步。
- 来源级限流、熔断和指标。
- 可选账号、跨设备收藏和进度同步。

## 11. 推荐的下一批开发任务

严格按顺序：

1. 建立持久化规范目录和分类索引。
2. 接入更多已授权 Native 来源并完成契约测试。
3. 拆分 `app/page.tsx` 的目录、详情和个人领域。
4. 完善清晰度重解析、播放失败自动换源和来源健康排序。
5. 建立榜单、排期和高级筛选 API。
6. 再扩展 JS/Browser 来源。
7. 接入有明确授权的弹幕通道。
8. 最后实现来源允许时的设备端视频缓存。
